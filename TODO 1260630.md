# TPT Boxcar — Master Task Checklist

> **License:** Apache 2.0 | **Repo type:** Monorepo | **Platform:** Linux · macOS · Windows (WSL2 for eBPF)
>
> **Progress:** 0 / 95 tasks complete

---

## Phase 0 — Foundation

- [ ] Initialize monorepo directory structure (`/origin`, `/tether`, `/scope`, `/chisel`, `/frontier`, `/docs`)
- [ ] Set up Cargo workspace (`Cargo.toml`) covering all Rust crates
- [ ] Set up Go workspace (`go.work`) covering all Go modules
- [ ] Add `LICENSE` (Apache 2.0)
- [ ] Add `CONTRIBUTING.md` with branch, commit, and PR conventions
- [ ] Configure Rust toolchain (`rust-toolchain.toml`) with stable + wasm32-wasi target
- [ ] Configure Go linting (`golangci-lint`)
- [ ] Configure Rust linting (`clippy.toml`, `rustfmt.toml`)
- [ ] Add root `README.md` with ecosystem overview and product links

---

## Product 1 — TPT Origin (Unified Local Sandbox)

### Core Engine (Rust)

- [ ] Design the manifest format — YAML/TOML schema for mixed OCI + Wasm workloads (services, ports, volumes, env vars)
- [ ] Implement manifest parser (serde-based, with validation and helpful error messages)
- [ ] Integrate containerd for OCI container lifecycle (pull, create, start, stop, delete) — no Docker daemon dependency
- [ ] Integrate Wasmtime for Wasm module execution (load `.wasm` file, configure WASI context, run)
- [ ] Implement eBPF-based local networking via Aya (Linux) — virtual network bridge, packet routing between services
- [ ] Implement macOS fallback networking (userspace bridge using `tun`/`vmnet` or similar)
- [ ] Implement Windows support — WSL2 for eBPF features; native CLI fallback for non-eBPF paths
- [ ] Implement local DNS resolver — map `<service-name>.local` to the correct container/Wasm address automatically
- [ ] Implement local service mesh — zero-config routing so containers and Wasm modules reach each other by name
- [ ] Implement lifecycle management — start, stop, restart, health-check polling, graceful shutdown
- [ ] Implement CLI: `tpt origin init` (scaffold manifest)
- [ ] Implement CLI: `tpt origin up` (spin up all services)
- [ ] Implement CLI: `tpt origin down` (tear down all services)
- [ ] Implement CLI: `tpt origin ps` (list running services with status)
- [ ] Implement CLI: `tpt origin logs <service>` (stream logs)
- [ ] Implement CLI: `tpt origin exec <service> <cmd>` (exec into container or Wasm instance)
- [ ] Write unit tests for manifest parser, DNS resolver, lifecycle manager
- [ ] Write integration tests — spin up a Postgres container + a Wasm module that connects to it

### Tauri Desktop GUI

- [ ] Design UI wireframes — dashboard, logs, manifest editor (lo-fi mockups acceptable)
- [ ] Scaffold Tauri project inside `/origin/gui` within the monorepo
- [ ] Implement dashboard view — list of running services with name, type (OCI/Wasm), status, CPU, RAM
- [ ] Implement log streaming view — per-service live log tail with search/filter
- [ ] Implement manifest editor — syntax-highlighted YAML/TOML editor with validation
- [ ] Implement one-click start/stop/restart controls per service
- [ ] Implement system resource panel — total CPU and RAM consumed by Origin runtime
- [ ] Package for Windows (`.msi` / NSIS installer)
- [ ] Package for macOS (`.dmg` with code signing placeholder)
- [ ] Package for Linux (`.AppImage` and `.deb`)

---

## Product 2 — TPT Tether (State & Connection Proxy)

### Core Proxy (Rust + Tokio)

- [ ] Design WIT definitions — WIT interface types for query, execute, transaction, key-value ops
- [ ] Implement WASI Preview 2 component model bindings (expose WIT interface to Wasm callers)
- [ ] Implement connection pool manager — configurable min/max pool size, idle timeout, acquire timeout
- [ ] Implement Postgres driver (sqlx-based) — full query/exec/transaction support over the pool
- [ ] Implement MySQL driver — same interface as Postgres driver
- [ ] Implement Redis driver (redis-rs) — key-value ops (get, set, del, expire, pub/sub)
- [ ] Implement TLS termination — mTLS between Tether and backend databases
- [ ] Implement database authentication handler — username/password, env-var secrets, Vault integration stub
- [ ] Implement connection health monitoring — ping loop, auto-evict broken connections, reconnect backoff
- [ ] Write unit tests for pool manager, each driver, TLS layer
- [ ] Write integration tests — Wasm test module ↔ Tether ↔ real Postgres/Redis (Docker Compose test env)

### Control Plane (Go)

- [ ] Design configuration schema (YAML: backends, pool settings, routing rules)
- [ ] Implement config API server (REST) — CRUD for backends and routing rules
- [ ] Implement Kubernetes ConfigMap watcher — hot-reload routing config on ConfigMap change
- [ ] Implement Consul KV integration — alternative config source for non-K8s environments
- [ ] Write OpenAPI spec for the config API
- [ ] Write end-to-end tests for control plane ↔ data plane config reload

---

## Product 3 — TPT Scope (Hybrid eBPF Observability)

### eBPF Collection Agent (Rust or Go)

- [ ] Design eBPF probe architecture — map kernel tracepoints to OTel semantic conventions
- [ ] Implement network traffic tracing probes (TCP connect/accept, UDP send/recv, bytes transferred)
- [ ] Implement system call tracing (open, read, write, exec — with process/container context)
- [ ] Implement Wasm runtime event tracing — hook Wasmtime events: compile time, instantiation latency, memory pages
- [ ] Implement OpenTelemetry exporter — emit traces, metrics, and logs via OTLP (gRPC + HTTP)
- [ ] Implement container-aware enrichment — attach container ID, image name, pod name to every span/metric
- [ ] Write unit tests for each probe; write integration test that traces a real HTTP request end-to-end

### Backend Storage

- [ ] Design ClickHouse schema — tables for traces (spans), metrics (time-series), logs
- [ ] Implement ingestion pipeline — OTel Collector → ClickHouse sink (via `clickhouse-go` or HTTP API)
- [ ] Configure retention policies — TTL-based expiry per data type (e.g., raw traces 7d, rolled-up metrics 90d)
- [ ] Implement query API layer (Go) — REST endpoints for dashboard to fetch traces, metrics, and logs

### Frontend Dashboard (React + TypeScript)

- [ ] Scaffold React + TypeScript + Vite project in `/scope/dashboard`
- [ ] Implement service dependency graph — Cytoscape or D3 force-directed graph of service call relationships
- [ ] Implement trace waterfall view — span timeline for a single distributed trace
- [ ] Implement metrics charts — latency (p50/p95/p99), throughput (req/s), error rate over time
- [ ] Implement Wasm-specific metrics panel — compile time, instantiation latency, memory usage per module
- [ ] Implement log viewer — filterable by service, severity, time range; full-text search
- [ ] Write E2E tests (Playwright) for the golden-path trace view and graph render

---

## Product 4 — TPT Chisel (Automated Distiller & Migrator)

### Core Engine (Go)

- [ ] Design the two-phase analysis pipeline — Phase 1: runtime trace; Phase 2: Wasm migration
- [ ] Implement secure container sandbox runner — spin up target container in isolated network, mount eBPF probes
- [ ] Implement eBPF file access tracer — record every `open()`, `read()`, `exec()` syscall during profiling run
- [ ] Implement runtime dependency mapper — build an exact list of files, libs, and syscalls actually used
- [ ] Implement distroless image builder — copy only used files into a minimal `scratch` or `distroless` base image
- [ ] Implement SBOM generator — output CycloneDX or SPDX 2.3 format listing all included components
- [ ] Integrate CVE scanner — scan the final image against a local Grype/Trivy database; fail on critical CVEs

### Wasm Migration Pipeline

- [ ] Implement language detector — inspect compiled binary / source files to identify Rust, Go, C++ workloads
- [ ] Integrate WASI SDK — invoke `wasi-sdk` toolchain for C/C++ cross-compilation to `wasm32-wasi`
- [ ] Integrate Emscripten — alternative C/C++ → Wasm path for web-compatible output
- [ ] Implement Rust wasm32-wasi pipeline — `cargo build --target wasm32-wasi`, strip, optimize with `wasm-opt`
- [ ] Implement Go Wasm compilation pipeline — `GOOS=wasip1 GOARCH=wasm go build`, validate output
- [ ] Implement output Wasm module validator — run module in Wasmtime sandbox, assert exit code and basic I/O

### AI / LLM Layer

- [ ] Integrate local LLM via Ollama API (target: Llama 3 8B) — analyze source code for Wasm incompatibilities
- [ ] Integrate cloud LLM via Claude API — same analysis task with higher quality output for complex cases
- [ ] Write prompt templates for: dependency analysis, unsafe code detection, suggested refactoring paths
- [ ] Implement structured output parsing — emit JSON list of incompatible symbols and suggested fixes
- [ ] Add local/cloud toggle via `chisel.yaml` config (`ai.backend: local | cloud`)

### CI/CD Plugins (distributable artifacts — NOT wired into this repo)

- [ ] Build GitHub Actions action (`action.yml` + Docker/JS runner) — users add it to their own repos
- [ ] Build GitLab CI component (`component.yml`) — same, distributable to user pipelines
- [ ] Write example workflow files and integration docs for both platforms

---

## Product 5 — TPT Frontier (Wasm-Native Edge Service Mesh)

### Data Plane (Rust)

- [ ] Design proxy architecture — listener, router, upstream pool, plugin chain
- [ ] Implement HTTP/1.1 handler (Hyper)
- [ ] Implement HTTP/2 handler (Hyper h2)
- [ ] Implement async I/O event loop (Tokio runtime, tuned for high connection count)
- [ ] Implement Wasm plugin loader (Wasmtime) — load `.wasm` plugin, expose host ABI
- [ ] Implement hot-reload of Wasm plugins — swap plugin in-place with zero dropped connections
- [ ] Implement routing rules engine — path prefix, header match, weighted round-robin
- [ ] Implement load balancing algorithms — round-robin, least-connections, consistent hashing
- [ ] Implement TLS termination — rustls-based, SNI routing, certificate auto-reload
- [ ] Implement bundled JWT validation Wasm plugin — verify RS256/ES256 tokens; configurable JWKS endpoint
- [ ] Implement bundled rate limiting Wasm plugin — sliding window, token bucket; configurable per-route
- [ ] Write benchmarks (`criterion`) — target: >100k req/s on a single core for simple proxy path

### Plugin SDK

- [ ] Design plugin host ABI — define the function signatures exposed to Wasm plugins
- [ ] Implement Rust plugin SDK crate — safe wrappers around the host ABI, example plugin template
- [ ] Implement TypeScript/AssemblyScript plugin SDK — same ABI, npm-publishable package
- [ ] Write example plugins (JWT, rate limit, custom auth) in both Rust and AssemblyScript
- [ ] Write plugin authoring documentation with build → test → hot-load walkthrough

### Control Plane (Go or Rust)

- [ ] Implement xDS v3 API server (ADS — Aggregated Discovery Service, Envoy-compatible)
- [ ] Implement Istio integration — consume Istio's xDS feed to configure Frontier as a sidecar proxy
- [ ] Implement Consul Connect integration — register services and pull intentions via Consul API
- [ ] Write E2E tests — real HTTP traffic through Frontier with a live Wasm plugin loaded

---

## Phase N — Cross-Cutting

- [ ] Set up documentation site (`/docs`) — Docusaurus with per-product sections and API references
- [ ] Write full flywheel integration test — Origin spins up app → Chisel distills image → Tether handles DB → Frontier routes traffic → Scope traces it all
- [ ] Set up community infrastructure — GitHub Discussions categories, issue templates, Discord server (optional)
- [ ] Commission security audit — static analysis (`cargo audit`, `govulncheck`), manual review of eBPF probes and Wasm sandboxing
